Immediate Debug:

------
-score
-combo garbage
X-levels
X-pause menu
X--if quit game, tell other console to quit too
-options menu
-game over menu
X-fade between screens
X--fadeout in main.c then fadein at beginning of each loop
-multiplayer combined audio
--only one console plays songs, other plays only sfx
X-multiplayer detect connection loss

Game Logic:
X-piece data structure
X-playfield matrix data structure
X-spawn piece
X-random piece selection / 7-bag logic
X-hold piece logic
X--todo: can't hold and unhold a piece before a new one locks
X---use a "hold available" var and reset it during a lock
X-collision detection
X-oob detection
X-add locked piece to playfield matrix
X-move many lines up or down playfield at once
X-check for cleared lines
X-move piece
X-rotate piece
X--matrix rotation
X--rotation offset tables
X---derive Arika wall kick data for I piece
X-gravity
X!!-soft drop piece
X--todo: soft drop doesn't move down by one, instead it amplifies gravity by 20x
X-hard drop piece
X-delayed auto shift / auto repeat (behaviour when holding movement key)
X--implement in move l/r
X--determine behaviour that stops/resets movement
X---only when no longer pressing
X---continues to next piece after lock down if still pressed
X-calculate time before piece locks
X--0.5s lock delay when gravity less than 20G?
X-increasing gravity
X-pause menu
X--iterate over framebuffer, dim everything 
X--add pause text
X-game over
X--conditions
X--game over screens
-scoring
--combo recognition
--perfect clear recognition
--t-spin recognition
-marathon game mode
--select starting level
-high score?

Multiplayer Game Logic:
X-win logic
-different gamemodes
--battle / attack / garbage
--highest score under time limit (time attack)
X-keeping game synced
X--no pause in mp
--in main menu, if you select mp, the text will flash until the other console also chooses mp
X--when both select mp, a ping will be sent which will be used to start the game at the same time
-garbage progress bar
X-add garbage to game
X--use shift_lines to move existing lines up before adding garbage at bottom
-additional scoring

Misc Logic:
-Logic for menus / options
X-System to switch between "screens" (game screen, menu screen, )

Graphics:
X-frame timing
-render menus
--option cycling and moving between options
--score and time in-game
X-render static game elements
X--arena, "hold", "next"
X-garbage progress bar
X-render pieces for hold
X-render pieces for next
X-render pieces in playfield matrix
X-render active piece
X-render ghost piece
X--disabled when no active piece
X--use hard drop function to get new position
-line clear effect

Text:
X-implement custom font
X-auto kerning and spacing

Display:
X-send display data to screen with DMA

Audio:
X-figure out how to get audio data to pwm using DMA
X-physical audio circuit
X--filtering: look at pwm lab
X---depends on output device?
X-figure out format of audio
X--4bit 40khz wav?
X--can we efficiently store 2 4bit samples in a byte?
X---i don't think so. dma only supports transferring one byte minimum at once
X--probably best: just use 8bit samples
X-figure out how to play multiple audio tracks at once for music + effects
X--physically combine outputs of 2 pwm circuits
X--combine them digitally by averaging signals
X---mix = ((sa - 128) + (sb - 128)) / 2;
-allow for multiple sound effects at once

Input:
X-physical connection for NES controller to proton
X--pcb or custom bought connector
X--choose ports that aren't used for other things
-detect whether plugged in
--controller disconnected = D0 pin low
--therefore every button will be read as pressed
--if every button is pressed, we can say that controller's not working
X-input polling / handling

Multiplayer Connection:
X-physical connection
X--usb-a to usb-a
X--can be tested without needing two protons by just connecting two sides of the same cable to different UART ports on the same proton
X-initiate communication
X--only if you select multiplayer menu?
X-message send logic
X-determine which messages need to be sent to enable gameplay
X--score / lines cleared