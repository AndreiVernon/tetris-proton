Immediate Debug:
-I piece rotation broken?
-when hit hold on a piece that is partially fallen, the new piece appears in its position
--has to do with confusing active_piece.shape with next_shape. make a new variable for this

Game Logic:
X-piece data structure
X-playfield matrix data structure
X-spawn piece
X-random piece selection / 7-bag logic
X-hold piece logic
X--todo: can't hold and unhold a piece before a new one locks
X---use a "hold available" var and reset it during a lock
X-collision detection
X-oob detection
X-add locked piece to playfield matrix
X-move many lines up or down playfield at once
X-check for cleared lines
X-move piece
X-rotate piece
X--matrix rotation
X--rotation offset tables
X---derive Arika wall kick data for I piece
X-gravity
X!!-soft drop piece
X--todo: soft drop doesn't move down by one, instead it amplifies gravity by 20x
X-hard drop piece
X-delayed auto shift / auto repeat (behaviour when holding movement key)
X--implement in move l/r
X--determine behaviour that stops/resets movement
X---only when no longer pressing
X---continues to next piece after lock down if still pressed
-calculate time before piece locks
--0.5s lock delay when gravity less than 20G?
-pause menu
-game over
X--conditions
--game over screens
-scoring
--combo recognition
--perfect clear recognition
--t-spin recognition
-marathon game mode
--select starting level
-high score?

Multiplayer Game Logic:
-win logic
-different gamemodes
--battle / attack / garbage
--highest score under time limit (time attack)
-keeping game synced
--send a sync frame over UART when the game starts so both render cycles work at the same time
--pause menu has to be frame-exact sync to avoid problems
---remove pause from multiplayer?
-garbage progress bar
-add garbage to game
--store garbage shapes using some data type
--use shift_lines to move existing lines up before adding garbage at bottom
-additional scoring

Misc Logic:
-Logic for menus / options
-System to switch between "screens" (game screen, menu screen, )

Graphics:
X-frame timing
-render menus
--option cycling and moving between options
--score and time in-game
X-render static game elements
X--arena, "hold", "next"
-garbage progress bar?
X-render pieces for hold
X-render pieces for next
X-render pieces in playfield matrix
X-render active piece
X-render ghost piece
X--disabled when no active piece
X--use hard drop function to get new position
-VFX?

Text:
-implement custom font
-auto kerning and spacing

Display:
-send display data to screen with DMA

Audio:
-figure out how to get audio data to pwm using DMA
-physical audio circuit
--filtering: look at pwm lab
---depends on output device?
-figure out format of audio
--4bit 40khz wav?
--can we efficiently store 2 4bit samples in a byte?
---i don't think so. dma only supports transferring one byte minimum at once
--probably best: just use 8bit samples
-figure out how to play multiple audio tracks at once for music + effects
--physically combine outputs of 2 pwm circuits
--combine them digitally by averaging signals
---mix = ((sa - 128) + (sb - 128)) / 2;

Input:
-physical connection for NES controller to proton
--pcb or custom bought connector
--choose ports that aren't used for other things
-initiate connection / detect whether plugged in
X-input polling / handling

Multiplayer Connection:
-physical connection
--usb-a to usb-a
--can be tested without needing two protons by just connecting two sides of the same cable to different UART ports on the same proton
-initiate communication
--only if you select multiplayer menu?
-message send logic
-determine which messages need to be sent to enable gameplay
--score / lines cleared

Physical design:
-?
