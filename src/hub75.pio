.program hub75
.side_set 1

; Pin Assignments (Configured in C):
; OUT pins (6): R1, G1, B1, R2, G2, B2
; SIDE-SET pin (1): CLK

.wrap_target
    out pins, 6    side 0  ; Output 6 data bits, drive CLK low
    nop            side 1  ; Drive CLK high (latch data into panel shift reg)
.wrap